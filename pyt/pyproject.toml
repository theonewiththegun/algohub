[project]
name = "pyt"
version = "0.1.0"
description = "A set of python algo implementations"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "pytest>=9.0.2",
]

